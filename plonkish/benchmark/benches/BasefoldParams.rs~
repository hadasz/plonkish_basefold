struct Q {}
impl plonkish_backend::pcs::multilinear::BasefoldExtParams for Q {
    fn get_rate() -> usize {
        return 2;
    }

    fn get_basecode() -> usize {
        return 2;
    }

    fn get_reps() -> usize {
        return 381;
    }
}
